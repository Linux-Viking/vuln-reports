# Vulnerability Intelligence Report
**Generated on:** {{ date }} | **Scanner:** {{ scanner_type }} | **Target:** {{ target_file }}


## 📊 Summary Statistics
| Metric | Value |
| :--- | :--- |
| **HOSTS** | {{ stats.total_hosts }} |
| **Total Unique CVEs** | {{ stats.total_cves }} |
| **Critical Risks (CVSS >= 9.0)** | {{ stats.critical_count }} |
| **KEV Exploited (Active)** | {{ stats.kev_count }} |

---

{% for cve_id, d in vulnerabilities.items() %}
## [{{ cve_id }}] {{ d.title }}
**CVSS Score:** `{{ d.cvss_score }}` | **KEV:** `{{ d.cisa_kev }}` | **Exploitability:** `{{ d.exploitability }}`

### 1. Description
{{ d.description }}

### 2. Affected Products
{% for a in d.affected -%}
- **{{ a.product }}**: {{ a.versions|join(', ') }}
{% endfor -%}
{% if not d.affected -%}
- N/A
{% endif %}

<div style="page-break-after: always;"></div>

### 3. Affected Hosts
{% for host in cve_to_hosts[cve_id][:5] -%}
- `{{ host[1] if host[1] else host[0] }}` ({{ host[2] }})
{% endfor -%}
{% if cve_to_hosts[cve_id]|length > 5 -%}
- *({{ cve_to_hosts[cve_id]|length - 5 }}+ more hosts)*
{% endif %}

<div style="page-break-after: always;"></div>

### 4. Remediation Recommendation
**{{ d.remediation }}**

### 5. Intelligence & References
{% if d.poc_link != 'N/A' -%}
- 🚀 **Exploit PoC Link**: [{{ d.poc_link }}]({{ d.poc_link }})
{% endif -%}
{% for ref in d.references[:4] -%}
- 🔗 [{{ ref }}]({{ ref }})
{% endfor %}

---
{% endfor %}

*Generated by Vuln-Reports Suite*
